<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Ignore machine-level Visual Studio fallback paths (e.g. from synced configs) that do not exist on this OS. -->
  <fallbackPackageFolders>
    <clear />
  </fallbackPackageFolders>
</configuration>
