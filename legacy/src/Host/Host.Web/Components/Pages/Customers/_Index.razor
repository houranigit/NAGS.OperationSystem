@*
  Feature shell: route + title only. The real UI lives in the data page (here: CustomersGrid).
  New features: copy this shape — _Index.razor hosts one primary grid/list/scheduler component.
*@
@page "/customers"

<PageTitle>Customers</PageTitle>

<CustomersGrid />
