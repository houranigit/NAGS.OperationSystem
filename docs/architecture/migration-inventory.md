# Migration Inventory

Single source of truth for the gradual rewrite from the old Blazor OperationsSystem (under `legacy/`) to v1.0.0.

Update a row whenever a feature's status changes. Status values: `not-started`, `in-progress`, `done`, `verified` (behavior confirmed against the old project).

## How To Use

1. When starting a feature, locate its row and set status to `in-progress`.
2. Confirm/expand the old reference files you used.
3. When the v1.0.0 feature is complete and tested, set `done`.
4. After comparing behavior against the old project, set `verified`.

Reference paths are relative to `legacy/src/`.

## Key Findings From The Legacy Census

- The legacy is already a DDD modular monolith (Result pattern, aggregates, value objects, snapshots, domain + integration events, outbox/inbox, vertical slices). The rewrite is primarily **legacy Blazor UI → API-first backend + a new clean Blazor (Auto + Radzen) portal**, not an architecture rebuild.
- **No `/api/v1`**: legacy exposes only `/api/identity/*`, `/api/notifications`, and `/api/mobile/v2`. Legacy Core/Store plus Contracts/Operations portal features have **no REST** today — every Blazor page needs an API-first equivalent in the rewrite.
- **Audit module is a stub**: real audit is automatic EF change capture into `audit.AuditTrails` (`BuildingBlocks.Infrastructure/Persistence/BaseDbContext.cs`, owned by legacy Core migrations). `SecurityEventType` is enum-only with no writers.
- **MasterData replaces legacy Core + Store**: catalog-only customers, staff members (legacy Employees), stations, services, tools, materials, and related records share one explicit v1.0.0 boundary. Stock and warehouse workflows require a future `Inventory` module rather than expanding MasterData.
- **Legacy roles admin UI missing**: the legacy had role APIs/queries but no `/system/roles` page. v1.0.0 now includes Roles CRUD and permission management in the Blazor portal.
- **Permissions are coarse** in legacy (Portal/Scheduler/Flights/Users/Roles/Sessions); master-data pages gated by `portal.manage`. v1.0.0 moves to `module.resource.action`.
- **Mobile has returned**: the Android app is rewritten at `src/Host/OperationsSystem.Mobile` (legacy offline engine + screens on the new API), served by a dedicated `/api/v1/mobile` BFF, `/hubs/mobile-sync` SignalR hub, and `auth/mobile/*` bearer endpoints. User-facing assignment alerts use the separate Notifications module and FCM transport.
- **No CSV/PDF/Excel exports** exist in the legacy. The rewrite now adds native filter-aware XLSX, CSV, and paginated PDF reports for the Flights list.
- Snapshots, per-station work-order numbering, contract numbering, advance-payment consumption, and the SignalR+FCM notification fan-out are the strong behaviors to preserve.

## Modules

### Identity

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| Authentication (login, refresh, logout, activate, change-password) | done | `Identity.Application/Commands/{Login,RefreshToken,Logout}/*`, `Identity.Presentation/Endpoints/AuthEndpoints.cs` | `src/Modules/Identity/**` (Features/Auth, Endpoints/AuthEndpoints) | JWT access + httpOnly refresh-cookie rotation; verified via integration tests |
| Account lifecycle (invite, resend invite, activate, update, deactivate) | done | `Identity.Application/Commands/{CreateUser,InviteUser,ResendInvitation,ActivateAccount,UpdateUser,UnlockUser,ChangePassword}/*`, `UserEndpoints.cs` | `Identity.Application/Features/Users/*`, `Identity.Api/Endpoints/UserEndpoints.cs` | Invitation flow with token; logging notifier (SMTP later) |
| User lockout | done | `Identity.Domain/Aggregates/User/{User.cs,PasswordHistoryEntry.cs}` | `Identity.Domain/Users/User.cs` | Auto-lock on failed attempts + manual lock/unlock. Password history deferred |
| Roles & permissions admin | done | `Identity.Application/Commands/{CreateRole,UpdateRolePermissions,AssignRole,RemoveRole}/*`, `RoleEndpoints.cs`, `Identity.Domain/Aggregates/Role/*`, `Authorization/Permissions.cs` | `Identity.Domain/Roles`, `Identity.Application/Features/Roles`, `Identity.Api/Endpoints/RoleEndpoints.cs` | New `module.resource.action` catalog; system roles protected; Blazor CRUD + permissions UI |
| Sessions (refresh-token tracking, rotation, revoke) | done | `Identity.Application/Commands/{RevokeSession,RevokeAllSessions}/*`, `SessionEndpoints.cs`, `Aggregates/UserSession/UserSession.cs` | `Identity.Domain/Sessions/UserSession.cs`, `Identity.Application/Features/Sessions/*`, `Identity.Api/Endpoints/SessionEndpoints.cs` | Issue/rotate/revoke + admin (list/revoke/revoke-all per user) and self-service (`/me/sessions` list, revoke, sign-out-others) endpoints; integration tests cover current-session marking and revoke flows |
| User queries (paginated users, role lookups) | done | `Identity.Application/Queries/{GetPaginatedUsers,GetAllRoleSelectOptions,GetRolesOverview}/*` | `Identity.Application/Features/{Users,Roles}/*Queries.cs` | Paged + search + status filter |
| Admin/role seeding + permission catalog | done | `Identity.Infrastructure/Seeding/{RoleSeeder.cs,AdminSeeder.cs}` | `Identity.Infrastructure/Seeding/IdentityDataSeeder.cs` | Seeds System Admin role (all perms) + bootstrap admin; `SystemUserId` |
| Identity integration events | done | `Identity.Contracts/IntegrationEvents/*`, `Identity.Application/IntegrationEvents/Handlers/EmployeeUserCreationRequestedIntegrationEventHandler.cs` | `Identity.Contracts/IntegrationEvents.cs`, `Identity.Application/Features/PortalAccess/*`, `Identity.Application/Features/Auth/ConfirmEmailChange.cs` | Outbox/inbox-driven (BuildingBlocks): consumes `PortalAccessRequested`/`LinkedRecordDeactivated`/`LinkedEmailChangeRequested` from MasterData and emits `PortalUserProvisioned`/`PortalUserDeactivated`; provisions invited User with `UserType`+`ExternalReferenceId`, enforces role↔UserType compatibility, deactivates/releases email + revokes sessions, email-reverification confirm flow |
| Portal access + lifecycle (cross-module) | done | (legacy Employee↔User events) | `MasterData.Application/Features/PortalAccess/*`, `MasterData.Application/Features/Customers/RemoveCustomerContact.cs`, `Identity.Application/Features/PortalAccess/*` | Grant access at create/later with a compatible Role via outbox/inbox; invitation provisioning; linked-email reverification; CustomerContact removal + released-email reuse (filtered unique index); deactivation propagation (Station/Customer/StaffMember/Contact → User) with session revocation; idempotent inbox; grant-access Blazor dialog on StaffMember/Customer-contact UIs; end-to-end + idempotency integration tests |
| Server-side data scope (fail-closed) | done | `Host.Web/Authorization/*` (coarse `portal.manage`) | `BuildingBlocks.Application/Abstractions/IUserContext.cs`, `BuildingBlocks.Api/Security/HttpUserContext.cs`, `MasterData.Application/Authorization/MasterDataScope.cs` | `UserType`+`ExternalReferenceId` claims on the JWT; MasterData query/command handlers restrict StationStaff to their Station and CustomerContact to their Customer, failing closed when the linked record or parent is missing/inactive (even mid-event); integration tests cover cross-scope denial and deactivation fail-closed |
| Permission-based authorization (API) | done | `Host.Web/Authorization/PortalPolicies.cs` | `BuildingBlocks.Api/Authorization/PermissionAuthorization.cs` | Dynamic per-permission policy provider; `RequirePermission(...)` |
| Blazor portal: auth + Users/Roles/Sessions screens | done | (Blazor `Components/Pages/Settings/System/**`) | `src/Host/OperationsSystem.Blazor/**` | Blazor Web App (Interactive Auto) + Radzen; bilingual EN/AR `.resx` + RTL; typed API client; permission-gated UI; login/MFA, account, users, roles, audit log, Master Data CRUD/wizards, portal-access controls |

### MasterData

Design source of truth: `docs/modules/master-data-foundation.md`.

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| Customer (+ contacts, optional user link) | done | `Core.Domain/Aggregates/Customer/*`, `Core.Application/Features/Customer/**` | `MasterData.*/Features/Customers` | Full stack: required active Country; optional 2-char IATA (normalized, indexed, non-unique) + optional 3-letter ICAO (filtered-unique); revised Address VO (separate table) with optional line2/region/postal; official email/phone; logo via IFileStorage (multipart endpoint); contact reconciliation by stable id with per-customer active-email uniqueness; add-contact endpoint; rowversion/ETag; `/api/v1/masterdata/customers` + permissions; typed client + list/detail/dialog + nested-contacts UI + nav; domain + integration tests. Contact portal access + LinkedUserId now wired via the portal-integration phase (grant-access UI + outbox/inbox provisioning) |
| StaffMember (+ working schedule, licenses, optional user provisioning) | done | `Core.Domain/Aggregates/Employee/*`, `Core.Application/Features/Employee/**` | `MasterData.*/Features/StaffMembers` | Full stack: required active Station + ManpowerType (app guards), unique normalized email (app + filtered DB index), optional `EmploymentContract` period (end ≥ start), optional `WorkingSchedule` 7-bit mask, license assignments reconciled by stable id with unique-license-type rule + uppercase number, rowversion/ETag, `/api/v1/masterdata/staff-members` + permissions, typed client + list/detail/dialog UI + nav + Station-detail nested staff section, domain + integration tests. Portal-access user provisioning now delivered in the portal-integration phase (grant-access UI + outbox/inbox provisioning + linked-email reverification + deactivation propagation). Detail page redesigned (2026-06-23) as a tabbed SaaS workspace (hero + Overview/Licenses/Schedule/Portal Access/Activity tabs, stat tiles, schedule chips, timeline-ready Activity placeholder) — frontend-only, no API change |
| Station | done | `Core.Domain/Aggregates/Station/Station.cs`, `Features/Station/**` | `MasterData.*/Features/Stations` | Full stack: 3-letter IATA (unique) + optional 4-letter ICAO (filtered-unique), required active Country (app + FK guard), name/city, rowversion/ETag, `/api/v1/masterdata/stations` + permissions, typed client + list/detail/dialog UI + nav, nested StaffMembers section scaffold, domain + integration tests |
| AircraftType | done | `Core.Domain/Aggregates/AircraftType/*`, `Features/AircraftType/**` | `MasterData.*/Features/AircraftTypes` | Full stack: manufacturer enum, normalized model, optional notes, rowversion/ETag, `/api/v1/masterdata/aircraft-types` + permissions, typed client + list/detail/dialog UI + nav, domain + integration tests |
| OperationType (incl. seeded Ad Hoc) | done | `Core.Domain/Aggregates/OperationType/*`, `Features/OperationType/**` | `MasterData.*/Features/OperationTypes` | Full stack: name/description catalog with protected system rows; seeded Ad Hoc operation type with stable id; rowversion/ETag; `/api/v1/masterdata/operation-types` + permissions; typed client + list/detail/dialog UI + nav; domain + integration tests |
| Service (incl. seeded Aircraft Per Landing) | done | `Core.Domain/Aggregates/Service/*`, `Features/Service/**` | `MasterData.*/Features/Services` | Full stack: name/description catalog with protected system rows; seeded Aircraft Per Landing service with a stable id; rowversion/ETag; `/api/v1/masterdata/services` + permissions; typed client + list/detail/dialog UI + nav; domain + integration tests. The former On Call seed is retired because On Call is derived in Operations |
| ManpowerType | done | `Core.Domain/Aggregates/ManpowerType/*`, `Features/ManpowerType/**` | `MasterData.*/Features/ManpowerTypes` | Full stack: domain + CQRS, EF + migration, unique name, rowversion/ETag, `/api/v1/masterdata/manpower-types` + permissions, typed client + list/detail/dialog UI + nav, domain + integration tests |
| License | done | `Core.Domain/Aggregates/License/*`, `Features/License/**` | `MasterData.*/Features/Licenses` | Full stack: domain + CQRS, EF + migration, unique uppercase alphanumeric code (immutable after create), rowversion/ETag, `/api/v1/masterdata/licenses` + permissions, typed client + list/detail/dialog UI + nav, domain + integration tests |
| Country | done | `Core.Domain/Aggregates/Country/*`, `Features/Country/**` | `MasterData.*/Features/Countries` | First MasterData slice complete: domain + CQRS, EF `masterdata` schema + migration, idempotent ISO-3166 seed, `/api/v1/masterdata/countries` with permissions + rowversion/ETag concurrency, typed Blazor client + list/detail/dialog UI + nav, domain + integration tests |
| Currency (+ exchange rates) | not-started | `Core.Domain/Aggregates/Currency/*`, `Features/Currency/**` | `MasterData.*/Features/Currencies` | Cross rates; seeded SAR/USD |
| Unit | not-started | `Store.Domain/Aggregates/Unit/*`, `Store.Application/Features/Unit/**` | `MasterData.*/Features/Units` | Measurement units; catalog only |
| Tool (+ equipment lines) | done | `Store.Domain/Aggregates/Tool/{Tool.cs,Equipment.cs}`, `Features/Tool/**` | `MasterData.*/Features/Tools` | Full stack: tool catalog with add/update/remove equipment rows reconciled by stable ids; no stock tracking; rowversion/ETag; `/api/v1/masterdata/tools` + permissions; typed client + list/detail/dialog UI + nav; domain + integration tests |
| Material | done | `Store.Domain/Aggregates/Material/*`, `Features/Material/**` | `MasterData.*/Features/Materials` | Full stack catalog only: name/description, lifecycle, rowversion/ETag, `/api/v1/masterdata/materials` + permissions, typed client + list/detail/dialog UI + nav, domain + integration tests. Stock behavior belongs in a future Inventory module |
| General support | done | `Store.Domain/Aggregates/GeneralSupport/*`, `Features/GeneralSupport/**` | `MasterData.*/Features/GeneralSupports` | Full stack catalog only: name/description, lifecycle, rowversion/ETag, `/api/v1/masterdata/general-supports` + permissions, typed client + list/detail/dialog UI + nav, domain + integration tests |
| Service price plans (bracketed) | not-started | `Core.Domain/Aggregates/ServicePricePlan/*`, `Features/ServicePricePlan/**` | `MasterData.*/Features/ServicePricePlans` | Tiered catalog pricing |
| Manpower price plans (bracketed) | not-started | `Core.Domain/Aggregates/ManpowerPricePlan/*`, `Features/ManpowerPricePlan/**` | `MasterData.*/Features/ManpowerPricePlans` | Tiered catalog pricing |
| Tool price plans (bracketed) | not-started | `Store.Domain/Aggregates/ToolPricePlan/*`, `Features/ToolPricePlan/**`, `Store.Domain/Events/ToolPricePlan*` | `MasterData.*/Features/ToolPricePlans` | Tiered catalog pricing |
| Material price plans (bracketed) | not-started | `Store.Domain/Aggregates/MaterialPricePlan/*`, `Features/MaterialPricePlan/**`, `Store.Domain/Events/MaterialPricePlan*` | `MasterData.*/Features/MaterialPricePlans` | Tiered catalog pricing |
| General support price plans (bracketed) | not-started | `Store.Domain/Aggregates/GeneralSupportPricePlan/*`, `Features/GeneralSupportPricePlan/**`, `Store.Domain/Events/GeneralSupportPricePlan*` | `MasterData.*/Features/GeneralSupportPricePlans` | Tiered catalog pricing |
| Catalog dashboard KPIs | not-started | `Core.Application/Features/Dashboard/Queries/GetCatalogDashboard/*` | `MasterData.Application/Features/Dashboard` | Home dashboard |
| MasterData seeding + well-known ids | done | `Core.Infrastructure/Seeding/CoreDataSeeder.cs`, `Core.Contracts/Seeding/CoreSeedIds.cs` | `MasterData.Infrastructure/Seeding`, `MasterData.Contracts/Seeding` | Current MVP baseline: idempotent ISO country seed plus protected Ad Hoc operation type and Aircraft Per Landing service rows with stable ids. On Call has no seed/id. Additional future catalog seeds can extend the same pattern |
| MasterData portal-access integration event handlers | done | `Core.Contracts/IntegrationEvents/*`, `Core.Application/IntegrationEvents/Handlers/*` | `MasterData.Application/Features/PortalAccess`, `MasterData.Contracts/PortalAccessEvents.cs` | Handles Identity provisioning, activation, restore, deactivation, email-change confirmation/failure, and provisioning failure replies with inbox idempotency and stale-correlation guards |
| Cross-module readers, snapshots, and name propagation | not-started | `Core.Contracts/Readers/*`, `Core.Infrastructure/Readers/*`, `Core.Contracts/Features/**`, `Store.Contracts/Readers/*`, `Store.Contracts/Features/**`, `Core.Contracts/IntegrationEvents/*` | `MasterData.Contracts`, `MasterData.Infrastructure/Readers` | Future modules should consume MasterData through contracts/read models/snapshots/events rather than internal entity references |

### Contracts

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| Contract create/update (wizard) | not-started | `Contracts.Domain/Aggregates/Contract/Contract.cs`, `Contracts.Application/Features/Contract/Commands/{CreateContract,UpdateContract}/*` | | Stations, OTs, 5 pricing line types, fees, advance payments |
| Contract lifecycle (suspend, activate/resume, terminate) | not-started | `Contracts.Application/Features/Contract/Commands/{SuspendContract,ActivateContract,TerminateContract}/*` | | Draft→Active→Suspended→Active→Expired/Terminated |
| Contract pricing lines (service/manpower/tool/material/general-support, brackets, overrides) | not-started | `Contracts.Domain/Aggregates/Contract/Pricing/*` | | Per-line package balances preserved across edits |
| Advance payments (per operation type, consumption) | not-started | `Contracts.Domain/Aggregates/Contract/ContractAdvancePayment.cs`, `ValueObjects/ScheduledAdvancedPayment.cs`, `Events/ContractAdvancePaymentConsumedEvent.cs` | | Balance/deposit consumption + shortfall events |
| Cancellation & delay charge plans (brackets) | not-started | `Contracts.Domain/Aggregates/Contract/{CancellationBracket.cs,DelayBracket.cs}` | | Penalty bracket tables |
| Contract numbering | not-started | `Contracts.Domain/.../ContractNo.cs` | | Human-readable contract number VO |
| Contract queries (by id, paginated, light, find-for-flight) | not-started | `Contracts.Application/Features/Contract/Queries/*` | | `FindContractForFlight` binds flight↔contract |
| Contract read service (for Operations) | not-started | `Contracts.Infrastructure/Readers/ContractReadService.cs` | | Surfaces `DebriefRequired` to WO approval |
| Contract status sync job | not-started | `Contracts.Infrastructure/BackgroundJobs/ContractStatusSyncJob.cs` | | Auto Draft/Active/Expired every 5 min |
| Contract expiring-soon notification job | not-started | `Contracts.Infrastructure/BackgroundJobs/ContractExpiringNotificationJob.cs` | | Every 6 hr |
| Contract integration events (lifecycle, expiring, advance balance) | not-started | `Contracts.Contracts/IntegrationEvents/*`, `Contracts.Application/IntegrationEvents/Handlers/*` | | Several handlers are placeholders |

### Operations

See `docs/modules/operations-foundation.md` (domain design, incl. the 2026-07-04 corrections) and `docs/modules/operations-module-plan.md` (build plan). The current branch baseline intentionally reset Operations to Flights first, then reintroduced Work Orders v1 Phase 1 with a cleaner model: no server drafts, owner-scoped active WOs, copied immutable flight context, approval locks the WO and settles the flight, return releases the current approval number and reopens the flight, and billing will read the approved WO directly instead of duplicated flight scalars.

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| Flight scheduling (schedule, batch schedule, update, change-number) | done | `Operations.Domain/Aggregates/Flight/Flight.cs`, `Operations.Application/Features/Flight/Commands/{CreateFlight,BatchCreateFlights,UpdateFlight}/*` | `Operations.Domain/Flights/Flight.cs`, `Operations.Application/Features/Flights/FlightCommands.cs` | Snapshots via MasterData read seam; original+current flight number; planned services mandatory (per-landing mixing rule); rowversion/If-Match; batch scheduling included |
| Per-Landing flights (claim, station-wide visibility, derived On Call) | done | `ClaimAogFlight/*`, `AutoAogWorkOrderJob.cs` | `Features/Flights/FlightLifecycleCommands.cs` (ClaimPerLandingFlight), `Features/WorkOrders/OnCallWorkOrderQuery.cs`, `Infrastructure/BackgroundJobs/AutoWorkOrderBackgroundService.cs` | Aircraft Per Landing = no-service designation; not performable as an actual service; station-wide visibility enforced server-side. On Call = any non-merged WO with a performed service line. Auto-WO background job creates review WOs for overdue Per-Landing flights using appsettings policy |
| Ad-hoc flights + work order from scratch | done | `CreateAdHocWorkOrderFromScratch/*` | `Features/WorkOrders/AdHocWorkOrderCommands.cs`, `Operations.Api/Endpoints/WorkOrderEndpoints.cs`, Blazor `AdHocWorkOrderDialog.razor` | Work Orders page can create an Ad Hoc flight and its first Completion/Cancellation WO in one transaction; duplicate candidates are surfaced before submit; planned services are required except explicit cancellation |
| Duplicate detection + merge (soft-archive) | done | (none in legacy) | `Features/Flights/FlightDuplicateDetector.cs`, `Features/Merge/MergeCommands.cs` | Scored ad-hoc dedupe; deterministic WO dedupe; Superseded/Merged, never hard-deleted |
| StaffMember flight assignment + assigned-only visibility | done | `InviteEmployeeToFlight/*`, `FlightAssignedEmployee.cs` | `Features/Flights/FlightCommands.cs` (AssignEmployees), `Application/Authorization/OperationsScope.cs` (EnsureFlightAccess), `Application/Common/FlightAssignmentEvents.cs` | Non-per-landing flights visible/authorable only by assigned staff (server-enforced on reads and writes); assigned staff can invite others; newly added roster members produce durable `FlightEmployeeAssigned` outbox events; self/unlinked/unassign cases are silent |
| Dispatcher visibility (station-wide without assignment) | done | `Identity.Infrastructure/Seeding/RoleSeeder.cs` (Dispatcher role), `Host.Web/Components/Pages/Scheduler/_Index.razor` (unscoped portal) | `Operations.Domain/Authorization/OperationsPermissions.cs` (`operations.flights.view-station`), `Application/Authorization/OperationsScope.cs` (`CanViewStationWide`) | Legacy dispatchers saw all flights unscoped; v1.0.0 splits: admin-type dispatcher sees all stations via data scope, station dispatcher (StationStaff + `view-station`) sees all own-station flights/work orders/review queue without assignment; lookup split mirrors legacy `scheduler.lookups.read` via `masterdata.reference.view-options` (all `/options` endpoints accept it OR the resource `.view`; new scoped lightweight `/staff-members/options`) |
| Work order authoring (owner-scoped; service lines, tasks, resources, attachments) | done | `WorkOrder.cs`, `WorkOrderTask*.cs`, `{CreateWorkOrderForFlight,UpdateWorkOrder,RecordReturnToRampLines}/*` | `Domain/WorkOrders/*`, `Features/WorkOrders/WorkOrderCommands.cs`, `Features/WorkOrders/WorkOrderAttachmentCommands.cs`, `Operations.Api/Endpoints/WorkOrderEndpoints.cs`, Blazor `WorkOrderEditorDialog.razor` | Submit/update/convert/delete before approval, one active non-merge WO per user/flight, immutable copied flight context, clear-and-rebuild service lines, stable-id task reconciliation, and task attachment upload/download/delete with storage cleanup. Per-Landing work orders start with zero service lines; staff add only performed services |
| Cancellation as work order | done | `CancelFlight/*` | `Domain/WorkOrders/WorkOrder.cs`, `Features/WorkOrders/WorkOrderCommands.cs` | Cancellation is a WO type; cancellation time required at submit; approval settles flight to Canceled |
| Work order review (approve, return/reopen) | done | `{ApproveWorkOrder,RejectWorkOrder,RevokeWorkOrder}/*` | `Features/WorkOrders/WorkOrderCommands.cs`, `Features/WorkOrders/WorkOrderServices.cs` | Approve assigns the first free station approval number, locks the WO, and settles the flight Completed/Canceled. Return requires a reason, releases number fields, records timeline entries, and reopens the flight to InProgress. No Reject command and no approved-WO scalar snapshot copied onto Flight |
| Per-station work order numbering | done | `StationWorkOrderCounter.cs`, `StationWorkOrderSequenceRepository.cs` | `Features/WorkOrders/WorkOrderServices.cs` (`WorkOrderNumberAllocator`) | Current approved display sequence `{IATA}-{nnnn}` with gap-filling reuse after return; permanent reference remains the WO Guid. `Operations_WorkOrders` adds the filtered active/approved uniqueness indexes plus station-scoped approval sequence uniqueness |
| Flight queries (list, by id, calendar, timeline) | done | `{GetFlightById,GetPaginatedFlights,GetFlightLightsInPeriod}/*` | `Features/Flights/FlightQueries.cs` | Station- and assignment-scoped; detail includes visible work-order summaries when caller has `operations.work-orders.view`; `GET /flights/{id}/timeline`. `IsOnCall`, service filters, exports, extraction, and approval revalidation share the non-merged-WO/any-service-line rule |
| Flight list exports (XLSX, CSV, PDF) | done | (none in legacy) | `Features/Flights/FlightQueries.cs`, `Operations.Api/Exports/FlightExportDocumentFactory.cs`, `Operations.Api/Endpoints/FlightEndpoints.cs`, Blazor `Features/Operations/Pages/FlightsList.razor` | Dedicated `operations.flights.export` permission; reuses the exact list scope, filters, and whitelisted sort without pagination; native styled workbook, RFC-style UTF-8 CSV, and paginated landscape PDF |
| Flight timeline / history | done | (none in legacy — WO review dialog only) | `Domain/Flights/FlightTimelineEntry.cs`, `Application/Common/FlightTimelineWriter.cs`, `operations.flight_timeline_entries` | Append-only, written in-transaction by lifecycle handlers; shown on flight detail page |
| Operations dashboard KPIs | done | `GetOperationsDashboard/*` | `Features/Dashboard/DashboardQueries.cs` + Blazor `Features/Operations/Pages/OperationsDashboardPage.razor` | |
| Blazor portal: dashboard, flights list, calendar, flight detail, WO dialogs, work orders list | done | (Host.Web pages incl. `Pages/Scheduler`) | `OperationsSystem.Blazor.Client/Features/Operations/**`, `Api/OperationsApiClient.cs` | Flight row/detail Open Work Order + Cancel Flight actions, work-order-first dialog, shared WO editor/view dialog with task attachments and signature capture, Work Orders list with approve/return/delete/merge actions, flight detail Work Orders tab, and merge dialog with field/source selections |
| Customer signature capture (work order) | done | `WorkOrder.cs` (CustomerSignature) | `Domain/WorkOrders/WorkOrder.cs`, `Features/WorkOrders/WorkOrderSignatureCommands.cs`, `WorkOrderAttachmentQueries.cs`, `Operations_WorkOrderSignatures` migration, Blazor `WorkOrderEditorDialog.razor` | Completion WOs can upload/replace/download/delete a PNG customer signature while editable; approval locks signature changes and WO deletion/update cleanup removes stored signature blobs |
| Mobile BFF endpoints (reads + idempotent writes) | done | `Operations.Presentation/Mobile/MobileV2Endpoints.cs` | `Operations.Application/Features/Mobile/*`, `Operations.Api/Mobile/{MobileReadEndpoints,MobileWriteEndpoints}.cs` | `/api/v1/mobile`: me, catalogs (via extended `IMasterDataReader` active lists), station roster, my/per-landing/ad-hoc flight lists (±12h STA window, embedded `myWorkOrder`), single-flight fetch, and idempotent `clientMutationId`/`clientFlightId` writes (submit/update/return-to-ramp WO, scratch ad-hoc, cancel, invite) recorded in `Operations_MobileMutations` atomically with the business change; StationStaff-only fail-closed scope; integration tests cover auth, reads, sync, idempotency, RTR, and cancel |
| Mobile offline sync (SignalR hub + REST catch-up) | done | `Operations.Presentation/Mobile/Sync/*`, `BuildingBlocks.Application/Abstractions/Mobile/Sync/*`, `Host.Web` `MobileSyncHub` | `BuildingBlocks.Application/Mobile/*`, `Operations.Api/Mobile/{MobileSyncHub,SignalRMobileSyncBroadcaster,MobileSyncEndpoints}.cs`, `Operations.Application/Features/Mobile/MobileFlightSync.cs` | Post-commit `change` envelopes (employee/station/all-stations audiences, `originMutationId` echo) emitted from flight/WO command handlers; `GET /api/v1/mobile/sync/changes` returns refresh envelopes per table; hub at `/hubs/mobile-sync` with JWT `?access_token=`; AOG renamed Per Landing (`flights-per-landing`) |
| Mobile bearer auth (refresh token in body) | done | `Identity.Presentation/Endpoints/AuthEndpoints.cs`, `Host.Web` `MobileJwt` policy | `Identity.Api/Endpoints/AuthEndpoints.cs` (`/auth/mobile/*`) | Login/MFA/refresh/logout reuse the web handlers + session rotation but exchange the refresh token via JSON body for secure device storage; integration-tested rotation, replay rejection, and logout revocation |
| Android app rewrite (offline-first client) | done | `legacy/src/Host/MobileApplication/**` | `src/Host/OperationsSystem.Mobile/**` | Kotlin/Compose app: legacy Room cache + FIFO outbox + SignalR/catch-up sync engine retargeted to `/api/v1` with the new flight/WO shape (normal planned-service prefill; Per-Landing work orders start empty and derive On Call after a performed line, string statuses, staff-member ids, task-id reconciliation, cancellation reason); Per Landing tab replaces AOG; MFA-aware login; DB v11 baseline (destructive upgrade from legacy contract) |

### Notifications

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| Notification inbox (list, unread count, mark read, mark all read, archive/all) | done | `Notifications.Domain/Aggregates/Notification/*`, `Notifications.Application/Features/{GetMyInbox,GetUnreadCount,MarkAsRead,MarkAllAsRead,ArchiveAll}/*`, `Notifications.Presentation/Endpoints/NotificationEndpoints.cs` | `Notifications.Domain/Notifications`, `Notifications.Application/Features/NotificationFeatures.cs`, `Notifications.Api/Endpoints/NotificationEndpoints.cs` | Bilingual persisted copy, generic payload, per-user read/archive state, auth-derived ownership |
| SignalR live delivery (web) | done | `Notifications.Presentation/Hubs/{NotificationsHub.cs,SignalRNotificationPusher.cs}` | `Notifications.Api/Realtime/*`, Blazor `Features/Notifications/*` | Dedicated authenticated hub at `/hubs/notifications`; portal bell/inbox/toast select EN/AR locally |
| Composite pusher abstraction | done | `Notifications.Infrastructure/Push/CompositeNotificationPusher.cs`, `Notifications.Application/Abstractions/I*NotificationPusher.cs` | `Notifications.Application/Abstractions/INotificationPusher.cs`, `Notifications.Infrastructure/Push/CompositeNotificationPusher.cs` | SignalR and FCM execute independently; failures surface only after all transports attempt, leaving `DeliveredAtUtc` unset so the source outbox retries with the stable notification id |
| Notification-from-event handlers | done | `Notifications.Application/IntegrationEvents/Handlers/FlightEmployeeInvitedIntegrationEventHandler.cs` | `Notifications.Application/IntegrationEvents/FlightEmployeeAssignedHandler.cs` | Inbox-idempotent consumer resolves `StaffMember.LinkedUserId`, suppresses self/unlinked recipients, persists then pushes |
| Device tokens + FCM push | done | `Notifications.Domain/Aggregates/DeviceToken/*`, `Notifications.Application/Features/{RegisterDeviceToken,RevokeDeviceToken}/*`, `Notifications.Infrastructure/Push/{FcmNotificationPusher.cs,FirebaseAppFactory.cs}` | `Notifications.Domain/Devices`, `Notifications.Application/Features/DeviceTokenFeatures.cs`, `Notifications.Infrastructure/Push/*`, Android `notifications/*` | Globally unique hashed token ownership, register/rebind/revoke lifecycle, high-priority data-only FCM, Android system alerts + cold/warm deep links |

### Audit

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| Automatic EF change-capture audit trail | done | `BuildingBlocks.Infrastructure/Persistence/BaseDbContext.cs`, `Persistence/Models/AuditTrail.cs`, `Core.Infrastructure/Persistence/CoreDbContext.cs` | `Audit.Infrastructure` (`audit.audit_trails`) | New Audit module. `AuditSaveChangesInterceptor` captures `IAuditable` create/update deltas into each module's outbox in the same transaction; the Audit consumer persists them (inbox-idempotent). User/Role marked `IAuditable` (MasterData aggregates pending Phase 3). |
| Business/security event audit | partial | `Audit.Domain/Enumerations/SecurityEventType.cs`, `Audit.Contracts/SecurityEvent/SecurityEventDto.cs` | `BuildingBlocks.Contracts.Auditing.AuditEntryRecorded` + `db.EnqueueAudit(...)` | Cross-cutting event + explicit-event helper available. Read API `GET /api/v1/audit/trails(/{id})` is admin-only and append-only; secrets redacted via `AuditRedaction`. Explicit lifecycle/login/MFA events expanded in Phase 2. |
| Audit read API + permissions | done | — | `Audit.Api`, `Audit.Domain/Authorization/AuditPermissions.cs` | `audit.trails.view` (SystemAdministrator-only); no write/delete endpoints; retained indefinitely for MVP. |

### BuildingBlocks (shared foundation)

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| CQRS abstractions + validation behavior | done | `BuildingBlocks.Application/Abstractions/**`, `Behaviors/{Validation,Authorization,Logging,Transaction}*` | `BuildingBlocks.Application/Messaging`, `BuildingBlocks.Application/Behaviors` | Command/query abstractions over MediatR plus Result-returning FluentValidation pipeline. Extra logging/transaction behaviors remain optional future work |
| Result/Error + domain primitives | done | `BuildingBlocks.Domain/{Results,Aggregates,Entities,ValueObjects,Events}/*` | `BuildingBlocks.Domain/**` | Result/Error pattern, aggregate/entity/value-object bases, domain events, and audit marker interfaces are in place |
| Outbox / Inbox processing | done | `BuildingBlocks.Infrastructure/Outbox/OutboxProcessorJob.cs`, `Persistence/Models/OutboxMessage.cs` | `BuildingBlocks.Application/Messaging`, `BuildingBlocks.Infrastructure/Messaging` | Per-module outbox/inbox tables, in-process integration dispatcher, idempotent inbox helpers, and Quartz outbox dispatch job |
| Integration event base | done | `BuildingBlocks.Contracts/IntegrationEvents/*` | `BuildingBlocks.Contracts/Messaging/IntegrationEvent.cs` | Stable integration-event base and handler contract |
| Email (SMTP + durable delivery) | done | `BuildingBlocks.Infrastructure/Email/*` | `BuildingBlocks.Infrastructure/Email` | SMTP sender, durable email outbox, content protection, and Identity notification adapters |
| Pagination | done | `BuildingBlocks.Application/Pagination/PaginatedResult.cs` | `BuildingBlocks.Application/Pagination` | `PagedResult<T>`, bounded `PageRequest`, and `SortSpec` support list endpoints |

### Host / Cross-cutting

| Feature | Status | Old reference files | New location | Notes |
|---|---|---|---|---|
| Module composition & wiring | done | `Host.Web/Program.cs`, `Extensions/ServiceCollectionExtensions.cs` | `OperationsSystem.Api/Program.cs` | Thin host; modular monolith (Audit, Identity, MasterData); health, rate limiting, correlation IDs, OpenTelemetry |
| Ordered migrations apply | done | `Host.Web/Extensions/DatabaseMigrationExtensions.cs` | `OperationsSystem.Api/Program.cs` | Startup applies Audit → Identity → MasterData → Operations → Notifications in dev; production uses reviewed scripts. First-release migration squash deferred to environment cutover. |
| Auth policies (portal cookie + MobileJwt) | done | `Host.Web/Authorization/PortalPolicies.cs`, `Extensions/WebApplicationExtensions.cs` | JWT Bearer + permission policies + UserType enforcement | Web portal uses JWT + refresh cookie; mobile deferred |
| OpenAPI / Scalar | done | `Host.Web/Program.cs` | `OperationsSystem.Api/Program.cs` | OpenAPI + Scalar in Development; `/api/v1` versioned business endpoints |
| Release hardening (health, CI, config validation, OTel) | done | — | `.github/workflows/ci.yml`, `docs/architecture/production-runbook.md`, `OperationsSystem.Api/OpenTelemetry/*` | Readiness/liveness probes, correlation IDs, outbox dead-letter cap, `IValidateOptions`, vulnerability audit gate, OpenTelemetry 1.15.3 |
