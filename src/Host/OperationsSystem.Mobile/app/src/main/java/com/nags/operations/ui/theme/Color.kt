package com.nags.operations.ui.theme

import androidx.compose.ui.graphics.Color

// NAGS brand palette — derived from the portal's deep-red "A" mark.
val BrandRed = Color(0xFF7E1414)
val BrandRedDark = Color(0xFF5A0E0E)
val BrandRedLight = Color(0xFFA52828)
val BrandRedTint = Color(0xFFFFE9E9)

// Neutral surfaces tuned to the brand red so cards never look cold.
val SurfaceWarm = Color(0xFFFFFAFA)
val SurfaceMuted = Color(0xFFF6EDED)
val OutlineSoft = Color(0xFFE3D5D5)
val OnSurfacePrimary = Color(0xFF1A1416)
val OnSurfaceSecondary = Color(0xFF5C4B4D)

// Semantic accents kept consistent with the portal's status colors.
val AccentBlue = Color(0xFF1E5BB8)
val AccentRed = Color(0xFFC52424)
